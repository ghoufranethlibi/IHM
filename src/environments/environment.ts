/**
 * Development environment — used by `ng serve` and non-production builds.
 * Paths are resolved via `angular.json` `fileReplacements` for production.
 */
export const environment = {
  production: false,
  apiUrl: 'http://localhost:8080/api'
};
